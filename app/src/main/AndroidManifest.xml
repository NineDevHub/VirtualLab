<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nyongconnect.android.virtuallab">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_atom"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_atom"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.StudentActivity" />
        <activity android:name=".activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AvailableExperimentActivity" />
        <activity
            android:name=".CanvasActivity"
            android:exported="true" />
        <activity android:name=".fragment.ReflectionActivity" />
        <activity android:name=".OhmsLawStudyActivity"></activity>
    </application>

</manifest>